<?php

namespace Tests\AppBundle\Command;

//use AppBundle\Command\AddUserCommand;

class AddUserCommandTest extends \PHPUnit_Framework_TestCase
{
    public function testAddUser()
    {
        /*$repo = null;

        $em = $this->getMockBuilder('AppBundle\Entity\UserRepository')
            ->setMethods(array('getRepository'))
            ->getMock();

        $em->expects($this->once())
            ->method('getRepository')
            ->with($this->equalTo('AppBundle:User'))
            ->will($this->returnValue($repo));

        $command = new AddUserCommand($em);*/
    }
}